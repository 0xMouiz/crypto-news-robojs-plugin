import { Config } from '../types/index.js';
import 'node:http';
import 'discord.js';
import './logger.js';

declare const DEFAULT_CONFIG: Config;
declare const FLASHCORE_KEYS: {
    state: string;
};
declare const STATE_KEYS: {
    restart: string;
};
declare const BUFFER: unique symbol;
declare const TIMEOUT: unique symbol;

export { BUFFER, DEFAULT_CONFIG, FLASHCORE_KEYS, STATE_KEYS, TIMEOUT };
