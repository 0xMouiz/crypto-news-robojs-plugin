import { Config } from '../types/index.js';
import 'node:http';
import 'discord.js';
import './logger.js';

/**
 * Returns the currently loaded configuration.
 * May return null if config has yet to load. Use `loadConfig` to load it first.
 */
declare function getConfig(): Config | null;
declare function loadConfig(file?: string): Promise<Config>;
declare function loadConfigPath(file?: string): Promise<string>;

export { getConfig, loadConfig, loadConfigPath };
